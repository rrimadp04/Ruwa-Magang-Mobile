name: frontend
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.2.2
  geolocator: ^13.0.2
  image_picker: ^1.1.2
  camera: ^0.11.1
  geocoding: ^5.0.0
  file_picker: ^8.1.6
  open_filex: ^4.5.0
  path_provider: ^2.1.5
  shared_preferences: ^2.5.5
  youtube_player_flutter: ^9.1.1
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/ringkasan_performa.png
    - lib/auth/screen/asset/logo.png
