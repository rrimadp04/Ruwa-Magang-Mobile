import 'package:flutter/material.dart'; import '../model/review_model.dart'; import 'logbook_ui.dart';
class ReviewCard extends StatelessWidget { const ReviewCard({super.key,required this.review, this.onTap}); final ReviewModel review; final VoidCallback? onTap; @override Widget build(BuildContext context)=>InkWell(onTap:onTap,borderRadius:BorderRadius.circular(16),child:Ink(padding:const EdgeInsets.all(16),decoration:logbookCardDecoration(),child:Row(crossAxisAlignment:CrossAxisAlignment.start,children:[CircleAvatar(backgroundColor:(review.approved?logbookSuccess:logbookWarning).withValues(alpha:.15),child:Icon(review.approved?Icons.check_rounded:Icons.rate_review_outlined,color:review.approved?logbookSuccess:logbookWarning)),const SizedBox(width:12),Expanded(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(review.reviewer,style:const TextStyle(fontWeight:FontWeight.w800,color:logbookInk)),Text(review.role,style:const TextStyle(fontSize:12,color:logbookMuted)),const SizedBox(height:6),Text(review.message,style:const TextStyle(color:logbookMuted,height:1.4))]))]))); }
