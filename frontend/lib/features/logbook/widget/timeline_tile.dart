import 'package:flutter/material.dart'; import 'logbook_ui.dart';
class TimelineTile extends StatelessWidget { const TimelineTile({super.key,required this.title,required this.subtitle,required this.done}); final String title,subtitle; final bool done; @override Widget build(BuildContext context)=>Row(crossAxisAlignment:CrossAxisAlignment.start,children:[Column(children:[Icon(done?Icons.check_circle_rounded:Icons.radio_button_unchecked_rounded,color:done?logbookSuccess:logbookWarning),Container(height:32,width:2,color:const Color(0xFFE2E8F0))]),const SizedBox(width:12),Expanded(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(title,style:const TextStyle(fontWeight:FontWeight.w700,color:logbookInk)),Text(subtitle,style:const TextStyle(fontSize:12,color:logbookMuted))]))]); }
